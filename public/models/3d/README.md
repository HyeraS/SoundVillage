# Future GLB runtime assets

Place exported runtime `.glb` files in this directory using the exact filenames in `docs/3d_asset_spec.md`. Placeholder geometry is used until models are enabled through `lib/three/assetManifest.mjs`.

Do not place `.blend` files, source textures, bake caches, or commercial game assets here.
